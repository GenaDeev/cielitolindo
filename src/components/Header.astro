---
import NavLink from "./NavLink.astro";
const { fitScreen } = Astro.props;
---
<header class="z-20 bg-celeste fixed w-full text-black flex justify-between items-center p-3 h-20">
    <a href="/" class="font-coco text-2xl">
        CielitoLindo
    </a>
    <nav class="flex items-center">
        <ul class="hidden sm:flex justify-around gap-4 p-2">
            <NavLink href="/nosotras" text="Nosotras"/>
            <NavLink href="/propuestas" text="Propuestas"/>
            <NavLink href="/contacto" text="Contacto"/>
        </ul>
        <!-- hamburger menu (preview) -->
        <svg xmlns="http://www.w3.org/2000/svg" class="sm:hidden h-10 w-10 bg-white/20 px-2 rounded-sm" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16" />
        </svg>
    </nav>
</header>